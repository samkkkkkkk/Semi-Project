<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.spring.semi.main.mapper.IPropertyMapper">
    <select id="getProperties" resultType="Property">
        SELECT ROWNUM, si_do, si_gun_gu,
		adstrd_nm,EXCHE_GTN, latitude,longitude
        FROM property 
        join administrative_code 
        on property.adstrd_nm =administrative_code.eup_myeong_dong 
        WHERE adstrd_nm = #{adstrdNm}
        <![CDATA[
         AND exche_gtn <= #{excheGtn}/#{area}
        ]]>
    </select>
</mapper>
